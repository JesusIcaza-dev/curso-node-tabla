# Notas:

Este es mi primer programa en node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Indica la base sobre la que se construira la tabla de
                 multiplicar                                 [number] [required]
  -l, --listar   Indica si quiere que se liste la tabla de multiplicar en la
                 consola                            [boolean] [default: "false"]
  -m, --maximo   Indica cuantas multiplicaciones se hara en la tabla de
                 multiplicar                              [number] [default: 10]
```
